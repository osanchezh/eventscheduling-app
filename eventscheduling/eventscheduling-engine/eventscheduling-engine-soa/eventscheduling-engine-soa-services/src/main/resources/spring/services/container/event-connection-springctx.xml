<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx"
    xmlns:rabbit="http://www.springframework.org/schema/rabbit"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
         http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
         http://www.springframework.org/schema/context 
         http://www.springframework.org/schema/context/spring-context-3.0.xsd
         http://www.springframework.org/schema/tx 
         http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
         http://www.springframework.org/schema/rabbit 
         http://www.springframework.org/schema/rabbit/spring-rabbit-1.3.xsd">

   <rabbit:connection-factory id="eventRabbitMQConnectionFactory"
      host="127.0.0.1"
      port="5672"
      virtual-host="eventscheduling"
      username="eventscheduling" 
      password="eventscheduling1"/>

    <bean id="rabbitConnectionFactory" class="com.rabbitmq.client.ConnectionFactory">  
        <property name="host" value="#{event_amqp_config_prop['eventscheduling.amqp.host']}" />  
        <property name="port" value="#{event_amqp_config_prop['eventscheduling.amqp.port']}" />  
        <property name="username" value="#{event_amqp_config_prop['eventscheduling.amqp.username']}" />  
        <property name="password" value="#{event_amqp_config_prop['eventscheduling.amqp.password']}" />  
        <property name="virtualHost" value="#{event_amqp_config_prop['eventscheduling.amqp.virtualhost']}" />  
        <property name="connectionTimeout" value="#{event_amqp_config_prop['eventscheduling.amqp.host']}" />  
    </bean>  
    
    
	<bean id="connectionFactory" class="org.springframework.amqp.rabbit.connection.CachingConnectionFactory">
	    <property name="channelCacheSize" value="50"/>
	</bean>


</beans>